node_modules
dist
.idea/

# Large binary manor assets are fetched separately, not versioned here.
**/manor/assets

# assets/manor/derived IS tracked: it is what the level actually loads, so a
# fresh clone runs without needing the raw manor pack. Regenerate it with
# `npm run prepare-assets` when the source textures change.

# macOS Finder cruft
.DS_Store
